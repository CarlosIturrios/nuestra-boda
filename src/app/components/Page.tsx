// src/app/components/Page.tsx

// Este es el componente HomePage, que representa la página de inicio de la aplicación.
// Es una estructura simple que renderiza un contenedor vacío para ser utilizado
// como parte de la interfaz de usuario.

const HomePage: React.FC = () => {
    return (
      // El div con id="home" y className="home" es un contenedor para el contenido de la página de inicio.
      // Este div se utiliza para renderizar la fotografia que ira en la portada.
      <div id="home" className="home"></div>
    );
  };
  
  export default HomePage;
  